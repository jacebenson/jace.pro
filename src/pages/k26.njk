---
title: K26
permalink: /k26/index.html
description: Connect with Jace Benson at K26
excludeFromSitemap: true
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="robots" content="noindex, nofollow">
  <title>{{ meta.author.name }} — K26</title>
  <meta name="description" content="Connect with {{ meta.author.name }} at K26">
  <link rel="canonical" href="{{ meta.url }}/k26">
  <link rel="icon" type="image/x-icon" href="/favicon.png">
  <style>
    :root {
      --bg: #0f0f12;
      --surface: #1a1a1f;
      --text: #f0f0f5;
      --muted: #a0a0b0;
      --accent: #dd4462;
      --accent-hover: #e86680;
      --radius: 16px;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html {
      background: var(--bg);
      color: var(--text);
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      -webkit-font-smoothing: antialiased;
    }

    body {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 2rem 1.5rem;
    }

    .card {
      width: 100%;
      max-width: 420px;
      background: var(--surface);
      border-radius: var(--radius);
      padding: 2rem;
      text-align: center;
      box-shadow: 0 20px 60px rgba(0,0,0,0.4);
    }

    .avatar {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      border: 3px solid var(--accent);
      margin-bottom: 1rem;
    }

    h1 {
      font-size: 1.75rem;
      font-weight: 700;
      margin-bottom: 0.25rem;
      letter-spacing: -0.02em;
    }

    .tagline {
      color: var(--muted);
      font-size: 0.95rem;
      margin-bottom: 1.5rem;
      line-height: 1.5;
    }

    .badge {
      display: inline-block;
      background: rgba(221, 68, 98, 0.15);
      color: var(--accent);
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.08em;
      padding: 0.35rem 0.75rem;
      border-radius: 999px;
      margin-bottom: 2rem;
    }

    .actions {
      display: flex;
      flex-direction: column;
      gap: 0.75rem;
      margin-bottom: 2rem;
    }

    .btn {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.6rem;
      width: 100%;
      padding: 1rem 1.25rem;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      font-size: 1rem;
      transition: transform 0.15s ease, background 0.2s ease;
      border: none;
      cursor: pointer;
    }

    .btn:hover {
      transform: translateY(-2px);
    }

    .btn:active {
      transform: translateY(0);
    }

    .btn-primary {
      background: var(--accent);
      color: #fff;
    }

    .btn-primary:hover {
      background: var(--accent-hover);
    }

    .btn-secondary {
      background: rgba(255,255,255,0.06);
      color: var(--text);
      border: 1px solid rgba(255,255,255,0.1);
    }

    .btn-secondary:hover {
      background: rgba(255,255,255,0.1);
    }

    .divider {
      height: 1px;
      background: rgba(255,255,255,0.08);
      margin: 1.5rem 0;
    }

    .links {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
    }

    .link-row {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0.75rem 0;
      text-decoration: none;
      color: var(--muted);
      font-size: 0.9rem;
      border-bottom: 1px solid rgba(255,255,255,0.05);
      transition: color 0.2s ease;
    }

    .link-row:last-child {
      border-bottom: none;
    }

    .link-row:hover {
      color: var(--text);
    }

    .link-row span:first-child {
      font-weight: 500;
    }

    .link-row span:last-child {
      font-size: 1.1rem;
    }

    .footer {
      margin-top: 2rem;
      color: var(--muted);
      font-size: 0.8rem;
    }

    .footer a {
      color: var(--muted);
      text-decoration: none;
    }

    @media (prefers-color-scheme: light) {
      :root {
        --bg: #f5f5f7;
        --surface: #ffffff;
        --text: #1a1a1f;
        --muted: #6e6e80;
      }
      .btn-secondary {
        background: rgba(0,0,0,0.04);
        border-color: rgba(0,0,0,0.08);
      }
      .btn-secondary:hover {
        background: rgba(0,0,0,0.08);
      }
      .link-row {
        border-color: rgba(0,0,0,0.06);
      }
      .divider {
        background: rgba(0,0,0,0.08);
      }
    }
  </style>
</head>
<body>

  <div class="card">
    <img src="/assets/images/me2.jpg" alt="{{ meta.author.name }}" class="avatar">
    <h1>{{ meta.author.name }}</h1>
    <p class="tagline">ServiceNow Architect · Maker of things</p>
    <div class="badge">Knowledge 2026</div>

    <div class="actions">
      <a href="mailto:{{ meta.author.email }}?subject=Nice%20meeting%20you%20at%20Knowledge%202026!&body=Hey%20Jace,%0A%0AGreat%20connecting%20at%20Knowledge%202026!%20I'd%20love%20to%20keep%20the%20conversation%20going.%0A%0AA%20bit%20about%20what%20caught%20my%20interest:%0A%F0%9F%92%A1%20Collaborating%20on%20something%20meaningful%0A%F0%9F%8E%A4%20Speaking%20at%20nullEDGE%0A%F0%9F%A4%96%20Learning%20more%20about%20AI%20in%20a%20Box%0A%F0%9F%91%8B%20Just%20genuine%20human%20connection%0A%0A" class="btn btn-primary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path><polyline points="22,6 12,13 2,6"></polyline></svg>
        Email Me
      </a>
      <a href="https://calendly.com/jacebenson/30min" target="_blank" rel="noopener" class="btn btn-secondary">
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect><line x1="16" y1="2" x2="16" y2="6"></line><line x1="8" y1="2" x2="8" y2="6"></line><line x1="3" y1="10" x2="21" y2="10"></line></svg>
        Book a 30-Min Call
      </a>
    </div>

    <div class="divider"></div>

    <div class="links">
      <a href="https://thenulledge.com" target="_blank" rel="noopener" class="link-row">
        <span>nullEDGE Conference</span>
        <span>&#8599;</span>
      </a>
      <a href="https://getaiinabox.com" target="_blank" rel="noopener" class="link-row">
        <span>AI in a Box</span>
        <span>&#8599;</span>
      </a>
    </div>
  </div>

  <p class="footer">
    <a href="{{ meta.url }}">{{ meta.domain }}</a>
  </p>

</body>
</html>
